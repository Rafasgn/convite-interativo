// Stub — motor da sequência de animação do tema Pequeno Príncipe (ADR 0009).
// Sequência: céu estrelado -> Príncipe olhando a lua -> raposa -> rosa -> envelope clicável.
// Ao clicar no envelope: revelação dos nomes (digitação) -> dados do evento -> confirmação (ADR 0008).
// Recebe os nomes dos convidados via injeção simples (ex.: data-nomes), sem configuração vinda do banco.
// Conteúdo real a implementar.
